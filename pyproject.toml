[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "mlops_agents"
version = "0.1.0"
dependencies = [
    "streamlit",
    "chromadb",
    "requests"
]

[tool.setuptools.packages.find]
where = ["."]
include = ["mlops_agents*"]